import Index from './home/index';


const routes = [
    {
        path: '/',
        name: 'main.index',
        component: Index,
        meta: {
            id: undefined
        }
    },

];

export default routes
